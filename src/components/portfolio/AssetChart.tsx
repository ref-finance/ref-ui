import React, { useEffect, useMemo, useState, useContext } from 'react';
export default function AssetChart() {
  useEffect(() => {}, []);
  return <div className="flex-grow bg-black bg-opacity-10"></div>;
}
